# Build-only Arch Linux ARM archive snapshot. Packages still require signatures
# from the pinned official Arch Linux ARM keyring; the guest uses mirrorlist.aarch64.
Server = https://pkgmirror.sametimetomorrow.net/aarch64/repos/2026/09/03/$repo
